table:
  name: gate_open_reason
  schema: public
select_permissions:
  - role: government
    permission:
      columns:
        - id
        - name
      filter: {}
      allow_aggregations: true
  - role: member
    permission:
      columns:
        - id
        - name
        - created_at
        - updated_at
      filter: {}
      allow_aggregations: true
