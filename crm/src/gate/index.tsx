import GateCreate from './GateCreate'
import GateEdit from './GateEdit'
import GateList from './GateList'

const gate = {
    name: 'gate',
    list: GateList,
    create: GateCreate,
    edit: GateEdit,
}

export default gate
