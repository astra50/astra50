import defaults from './defaults'

const gate_open_reason = {
    name: defaults.reference,
    options: {
        label: defaults.label,
    },
    recordRepresentation: 'name',
}

export default gate_open_reason
