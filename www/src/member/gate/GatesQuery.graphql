fragment GateWithLastOpen on gate {
    id
    name
    number
    delay
    opens(order_by: {created_at: desc}, limit: 1) {
        created_at
    }
    cctv {
        url
        preview
    }
}

query Gates {
    gate(order_by: {number: asc}) {
        ...GateWithLastOpen
    }
}
