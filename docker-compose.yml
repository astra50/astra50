version: '3.7'

services:
    react-admin:
        image: node:14.18.0-alpine3.12
        extra_hosts:
            - crm-admin.astra50.local:0.0.0.0
        labels:
            ru.grachevko.dhu: 'crm-admin.astra50.local:2'
        command: yarn start
        environment:
            HOST: crm-admin.astra50.local
            PORT: 80
        volumes:
            - ./:/app
        working_dir: /app
